// Criando uma nova instância do Vue
const vm = new Vue({
    // Definindo o elemento no DOM ao qual o Vue será vinculado
    el: '#app',

    // Definição dos dados do componente (atualmente vazio)
    data: {},

    // Definição dos métodos do Vue
    methods: {
        selecionarElementoFilho() {
            console.log(inputTexto.type)             // Obtém o tipo do input
            // console.log(opcao.value)              // Obtém o valor selecionado do select
            // console.log(paragrafo.style.color)    // Obtém a cor do parágrafo
            // console.log(recupradiv)              // Obtém o elemento <div> referenciado


        }
    }
})
