const vm = new Vue({
    el: '#app', // Define o elemento DOM onde o Vue será montado (um elemento com id="app").
    data: { 
        mensagem: 'Estudando a Diretiva v-for Implementando laço de repetição', // Texto a ser exibido no título.
        cursos: ['Laravel', 'Web Completo', 'Banco de Dados', 'Solid', 'Angular'], // Array de objetos representando cursos.]
    },
    methods: {
        
    }   
});